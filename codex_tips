高优先级：视觉主循环、控制回调、TCP 图传线程在共享同一批非原子状态，先改这个最值。project/user/main.cpp:67 里主循环调用 image_test()；project/code/image.cpp:563 同时写 image_copy、g_track_info、
    vision_target_yaw；project/code/scheduler.cpp:82 又在定时回调里直接读这些值；project/code/scheduler.cpp:125 的后台线程还在无锁发送图像，注释里自己也写了“没有加锁”。这会导致偶发错帧、半更新边界、控制抖
    动。
  - 高优先级：image.cpp 已经是典型 god module，职责太多，后续任何算法调整都会继续往里糊。project/code/image.cpp:4 先定义了一堆全局状态；project/code/image.cpp:187 到 project/code/image.cpp:633 又把采图、阈
    值/边界提取、中线滤波、偏差估计、场景分类、目标航向生成全塞在一个模块里。这里最适合拆成 FrameGrabber、TrackExtractor、VisionResult 三层。
  - 高优先级：模块边界基本靠 extern 硬穿透，调试状态也污染了业务逻辑。project/code/image.hpp:6 暴露了整套图像和边线缓存；project/code/scheduler.hpp:9 暴露调度器和 PWM 状态；project/user/main.hpp:6 暴露
    test1/test2/test3；更离谱的是 project/code/speed_strategy.cpp:25 里速度策略还在回写 test3。这类代码看起来“方便”，但长期结果就是谁都能读谁都能改，完全没法做局部重构。
  - 中优先级：状态机和实际行为已经漂移了，很多分支是“看上去有，实际上没走到”。project/code/speed_strategy.hpp:10 定义了 LostLine，但 project/code/image.cpp:609 实际只会产出 Straight/GentleCurve/SharpCurve；
    project/code/scheduler.cpp:26 的 target_speed/target_yaw 也已经偏半废弃；project/code/image.cpp:30 的断点标志和圆环检测函数同样没有形成完整闭环。先删掉死分支，代码会清爽很多。
  - 中优先级：main.cpp 把正式流程、人工调试、设备参数写死到一起了。project/user/main.cpp:48 里 IP 写死；project/user/main.cpp:65 曝光写死；project/user/main.cpp:70 键盘热键长期挂着；project/user/main.cpp:71
    还直接拿 test1 去推 ESC。建议至少分出 AppConfig 和 DebugConsole，让比赛运行模式和实验模式可切换。
  - 中优先级：构建系统在放大历史包袱。project/user/CMakeLists.txt:79 用 aux_source_directory 全目录扫源码，结果 project/code/image_test.cpp:1 这种整文件注释掉的历史实现还挂在仓库里。短期没炸，长期一定会
    把“备份代码”和“正式代码”混脏。

  假设

  - 这是静态审查，我没跑编译和上板测试。
  - 我按当前入口判断 master_timer 回调、主循环、TCP 线程是并发关系；如果底层其实串行，并发问题会降级，但当前代码和注释都在按并发模型写。

  建议顺序

  1. 先做 VisionResult 快照，把视觉输出改成“生成结果结构体”，控制线程只读快照，TCP 图传改双缓冲或加锁。
  2. 再拆 image.cpp，同时收掉 extern，把 test1/test2/test3/key_mode 赶出业务模块。
  3. 最后清理 CMake 和 dead code，把 image_test.cpp 这类历史残留移走。

  如果你要，我可以直接按这个顺序先帮你做第一刀重构，把最脏的共享状态收起来。



//===============
时间：2026-04-22 21:29:16 CST
用户问题：编译出现问题。
Codex建议：
1. 已定位到 `project/code/image.cpp` 中 `map_dev_to_angle(float)` 被重复定义了两次。
2. 已删除后面那份重复定义，保留文件前部的 `static float map_dev_to_angle(float deviation)` 版本。
3. 这类错误的特征就是：`redefinition of ...`，通常表示同一个 `.cpp` 文件里把同名函数又贴了一遍。

//===============
时间：2026-04-22 21:19:07 CST
用户问题：对于直角弯，0.03 之前都走直线，0.125 差不多要 5 度，0.3 差不多要 15 度，0.35 要 45 度，0.6 要 75 度，重新计算曲线。
Codex建议：
1. 这组点位不适合一条全局三次曲线，最稳的是“死区 + 分段曲线”。
2. 推荐映射：
   - `x <= 0.03`：`y = 0`
   - `0.03 < x <= 0.125`：`t = (x - 0.03) / 0.095`，`y = 5 * t * t`
   - `0.125 < x <= 0.3`：`u = (x - 0.125) / 0.175`，`y = 5 + 10 * (u * u * (3 - 2 * u))`
   - `0.3 < x <= 0.35`：`v = (x - 0.3) / 0.05`，`y = 15 + 30 * (v * v * (3 - 2 * v))`
   - `0.35 < x <= 0.6`：`w = (x - 0.35) / 0.25`，`y = 45 + 30 * (w * w * (3 - 2 * w))`
   - `x > 0.6`：`y = 75`
   - 最后 `return copysignf(y, deviation);`
3. 这条曲线精确满足：
   - `0.03 -> 0°`
   - `0.125 -> 5°`
   - `0.3 -> 15°`
   - `0.35 -> 45°`
   - `0.6 -> 75°`
4. 如果你觉得 `0.3 ~ 0.35` 这一段还要更猛，可以只把第三段改成 `y = 15 + 30 * (1 - (1 - v) * (1 - v) * (1 - v))`，这样会更早冲向 45 度。

//===============
时间：2026-04-22 21:02:13 CST
用户问题：deviation 在 0.2 之前转向小一点，在 0.2 到 0.5 之间迅速增加；0.2 大约对应 45 度，0.5 大约对应 80 度，怎么给拟合曲线？
Codex建议：
1. 这个需求不适合硬套一条全局三次曲线，更适合直接用“分段曲线”。
2. 推荐先用下面这条简单分段：
   - 设 `x = fabs(deviation)`；
   - `0 <= x <= 0.2`：`y = 45 * (0.15 * t + 0.85 * t * t)`，其中 `t = x / 0.2`
   - `0.2 < x <= 0.5`：`y = 45 + 35 * (2 * u - u * u)`，其中 `u = (x - 0.2) / 0.3`
   - `x > 0.5`：`y = 80`
   - 最后 `return copysignf(y, deviation);`
3. 这条曲线的特点：
   - `0.2` 前增益更温和；
   - `0.2 ~ 0.5` 用二次 ease-out，增长明显更快；
   - `0.2 -> 45°`，`0.5 -> 80°` 是精确满足的。
4. 如果你觉得 `0.2 ~ 0.5` 还不够猛，就把第二段改成更激进的：
   - `y = 45 + 35 * (1 - (1 - u) * (1 - u) * (1 - u))`
   这样在 `0.3` 和 `0.4` 附近会更快逼近 80 度。

//===============
时间：2026-04-22 20:43:27 CST
用户问题：直接说 Weight1 还有 Weight2 分别处理多少行。
Codex建议：
1. 从代码循环范围看，`Cal_Weigth1()` 和 `Cal_Weigth2()` 都只处理 `i = 1 ~ 99`，也就是各自都参与了 99 行。
2. 如果说“真正有权重作用的非零行”：
   - `Weight1` 实际非零有效区大约是 `row 46 ~ 99`，共 54 行，更偏近处。
   - `Weight2` 实际非零有效区大约是 `row 26 ~ 89`，共 64 行，更偏远处。

//===============
时间：2026-04-22 20:11:22 CST
用户问题：让我直接在工程里简单修改，避免之前键盘输入方案把终端会话搞乱。
Codex建议：
1. 已将 `project/user/main.cpp` 里的 `fcntl(... O_NONBLOCK)` 相关逻辑移除，改为只使用 `termios` 的 `ICANON/ECHO` 关闭和 `VMIN=0/VTIME=0` 方式。
2. 现在 `keyboard_poll_simple()` 仍然可以在主循环里低频轮询按键，但退出后不再把 shell 的文件描述符状态一并污染。
3. 这个修改主要是修复“退出后旧终端 CPU 异常/会话异常”的问题，不会显著降低图像链路本身的 CPU 占用。

//===============
时间：2026-04-22 20:08:50 CST
用户问题：重新开一个终端又好了。
Codex建议：
1. 这基本可以确认，之前“退出后 CPU 还是 100%”不是整台系统都卡住了，而是旧终端会话被污染了。
2. 最可疑的就是 `keyboard_init_simple()` 里对 `STDIN_FILENO` 设置了 `O_NONBLOCK`，退出时旧 shell 继承了这个状态，导致终端进程在输入处理上异常空转；新开一个终端会重新创建会话，所以就恢复正常。
3. 这也说明当前最该改的不是图像部分，而是键盘输入这块的实现方式。最稳的修法仍然是：去掉 `fcntl(... O_NONBLOCK)`，改成 `select()` 检查是否可读，再 `read()`。
4. 如果你后面还想继续保留“简单按键改变量”的方案，我建议下一步只做这一处收敛修改，不要再继续在 `O_NONBLOCK` 这条路上叠补丁。

//===============
时间：2026-04-22 19:55:28 CST
用户问题：烧进去运行后 CPU 一直 100%，退出程序后还是 100%。
Codex建议：
1. 这个现象要分开看：
   - 运行时 100%：更像是摄像头抓帧、OpenCV 转灰度、图像处理和图传线程本身造成的，不像是简单按键轮询造成的。
   - 退出后还是 100%：很像 `keyboard_init_simple()` 把 `STDIN` 设成了 `O_NONBLOCK`，但退出时没有成功恢复，导致 shell 继承非阻塞输入后空转。
2. 当前代码里 `system_delay_ms(10)` 本身没问题，它就是 `usleep(time * 1000)`，不是忙等。
3. 定时器线程也不是忙等，它内部是 `epoll_wait()` 阻塞。
4. 摄像头取帧 `wait_image_refresh()` 用的是 `cap >> frame_mjpg`，再加上每轮 `cvtColor`、`image_test()` 和后台 `seekfree_assistant_camera_send()`，跑满一个核是完全有可能的。
5. 如果你现在最想先验证“退出后 100%”是不是键盘代码导致的，最直接的测试就是：
   - 先临时把 `keyboard_init_simple();` 注释掉再跑一次；
   - 或者退出后在终端手动执行 `stty sane` 和 `stty -icanon echo` 的反向恢复，再看 CPU 是否立刻回落；
   - 更关键的是执行 `fcntl(STDIN_FILENO, F_SETFL, g_old_flags);` 是否真的走到了。
6. 如果你后面想保留最小改动方案但避免污染 shell，最稳的是：
   - 不再改 `O_NONBLOCK`；
   - 改用 `select()` 先等 0 超时检查 `STDIN` 是否可读；
   - 可读时再 `read()`。
   这样退出后不会把 shell 的文件描述符标志改乱。

//===============
时间：2026-04-22 19:47:28 CST
用户问题：让我看一下当前这种最小改动的键盘输入写法可不可以。
Codex建议：
1. 当前思路是对的，`main()` 里初始化一次，主循环里轮询一次，这个接法是合理的。
2. 但现在有一个直接问题：`keyboard_restore_simple()` 被声明了、也被调用了，但没有真正定义，当前代码会在链接阶段报错。
3. 还有一个行为问题：`'s'` 分支里只是单次调用 `motor_stop()`，但调度器还在持续执行 `motor_set_speed(...)`，所以这一下停车很可能只维持极短时间，马上又被控制环覆盖。
4. 如果只是想保留最小改动方案，优先补上 `keyboard_restore_simple()`，再把 `'s'` 改成“切一个停车标志位”而不是只调用一次 `motor_stop()`。

//===============
时间：2026-04-22 19:05:51 CST
用户问题：觉得线程方案改动太多，想结合当前框架少加一点东西，实现简单按一个键就做简单操作，比如变量加加。
Codex建议：
1. 对你现在的工程，最简单的办法不是单独开线程，而是“终端设为 cbreak + 非阻塞读键盘”，然后直接在现有 `while(1)` 里每轮调用一次 `read()`。
2. 你主循环本来就有 `system_delay_ms(10)`，所以这种写法虽然本质上是低频轮询，但 CPU 开销已经很低，通常够用，而且改动非常小。
3. 最少改动方案只需要：
   - 在 `main.cpp` 增加 `termios` 和 `fcntl` 头文件；
   - 增加 `keyboard_init_simple()` / `keyboard_restore_simple()` / `keyboard_poll_simple()` 三个函数；
   - 在 `main()` 初始化里调用一次 `keyboard_init_simple()`；
   - 在 `while(1)` 里加一行 `keyboard_poll_simple()`；
   - 在 `cleanup()` 里恢复终端。
4. 这种方法最适合“按一个键就变量加加/减减、切个标志位、临时停车”这种简单操作，不适合以后扩展成很复杂的交互系统。

//===============
时间：2026-04-22 18:30:58 CST
用户问题：把终端设为非规范模式（raw/cbreak），单独线程阻塞 `read(STDIN_FILENO, &ch, 1)` 这种方法详细讲一下，并给出完整代码和所放位置。
Codex建议：
1. 对你这个场景，推荐 `cbreak`，不推荐一上来就用完全 `raw`。因为 `cbreak` 只关闭行缓冲和回显，`Ctrl+C` 这类终端信号仍然保留；而 `raw` 会把 `Ctrl+C` 也当普通字符，调试会不方便。
2. 这套方案的核心是：主控制线程继续跑车，单独开一个键盘线程阻塞 `read(STDIN_FILENO, &ch, 1)`。因为是阻塞等待，不会忙轮询，CPU 占用极低。
3. 键盘线程只负责“收按键并写模式请求变量”，不要在键盘线程里直接改电机、停定时器、改复杂状态。真正切模式放在主循环里做，这样更稳。
4. 如果你要在当前工程里最少改动，最简单的放置方式就是直接把终端配置、键盘线程和模式切换检查逻辑都放进 `project/user/main.cpp`，不需要额外改 CMake。
5. 完整实现建议包含四部分：
   - 终端模式初始化与恢复：`termios`
   - 全局模式请求变量：`std::atomic<int>`
   - 键盘线程：阻塞 `read()`
   - 主循环里消费模式请求并执行实际切换

//===============
时间：2026-04-22 18:26:30 CST
用户问题：增加规则：每次在修改 code_tips 的时候在最上面增加一行 `//===============`，并且本次回答的答案下面多空几行。
Codex记录：后续每次修改 `codex_tips` 时，都会在文件最上面先增加一行 `//===============`，并按原有规则继续写入时间、问题和建议。

时间：2026-04-22 18:21:00 CST
用户问题：在终端输入 Ctrl+C 算是一种信号让程序退出，那是不是也可以按别的按键作为不同的信号，在程序运行过程中切换模式？这样写会不会比按键轮询更省 CPU、效果更好？
Codex建议：
1. `Ctrl+C` 确实是终端发给前台进程组的 `SIGINT`。你现在就在 [main.cpp](/home/loren/桌面/LS2K0300_Library/LS2K300_Library/Seekfree_LS2K0300_Opensource_Library/project/user/main.cpp#L43) 注册了这个信号处理。
2. 但“普通按键”默认不是信号。终端里只有少数控制键会触发信号，例如：
   - `Ctrl+C` -> `SIGINT`
   - `Ctrl+\\` -> `SIGQUIT`
   - `Ctrl+Z` -> `SIGTSTP`
   普通的 `a`、`1`、`q` 这类键只是标准输入字符，不会自动变成信号。
3. 如果你想在跑车过程中切模式，最推荐的不是“多用信号”，而是“单独开一个阻塞式输入线程”或者用 `select/poll` 阻塞等输入。关键点是“阻塞等待”，不是忙轮询。阻塞读键盘时 CPU 占用几乎可以忽略，不会比信号方案差。
4. 真正决定 CPU 占用高低的，不是“信号还是按键”，而是“阻塞等待还是高频轮询”：
   - 忙轮询按键：浪费 CPU，不推荐；
   - 阻塞式读按键：CPU 很低，推荐；
   - 信号：也低频，但不适合承载复杂模式逻辑。
5. 信号更适合“少量异步事件”，比如退出、急停、暂停、恢复；不适合做很多模式切换。原因是：
   - 可用信号种类有限；
   - 信号处理函数里能安全做的事情很少；
   - 模式逻辑一复杂就很别扭。
6. 如果你只想要 1 到 2 个外部切换动作，可以考虑 `SIGUSR1` / `SIGUSR2`，但通常是通过 `kill -USR1 pid` 这种方式发，不是直接按普通键。
7. 如果你想在当前终端里按 `1/2/3/q` 切模式，建议方案是：
   - 把终端设为非规范模式（raw/cbreak）；
   - 单独线程阻塞 `read(STDIN_FILENO, &ch, 1)`；
   - 线程里只把“模式请求”写进原子变量；
   - 主控制循环看到请求后再正式切模式。
8. 还有一个工程细节：信号处理函数里不要做太多事。你现在的 `sigint_handler()` 里直接 `printf`、`motor_stop()`、`exit()`，能跑但不算最稳。更好的写法是“信号里只置一个退出标志”，真正停定时器、停电机、释放资源放回主循环或清理路径里做。






//======================================================================
时间：2026-04-22 18:05:03 CST
用户问题：我的所有关于线的数组都是从图像最底层往上填充数据的吗？还有我的 weight 函数哪一个更近、哪一个更远一点？
Codex建议：
1. 你的行坐标体系确实是“`row` 越大越靠近图像底部，`row` 越小越靠近图像顶部”。这一点从 `search_line()` 里 `for (row = row_max; row >= row_min; row--)` 可以确认，左右边线是按从底往上扫描得到的。
2. 但不是“所有数组都直接从底往上采样填充”。更准确地说：
   - `reference_point` 用的是底部附近的 `REFERENCEROW` 行；
   - `reference_col` 是按列从底往上搜出来的；
   - `left_edge_line` / `right_edge_line` 是逐行从底往上扫线得到的；
   - `mid_line` 不是重新扫出来的，而是后处理时用 `mid_line[row] = (left_edge_line[row] + right_edge_line[row]) / 2` 逐行算出来的；
   - `reference_col_line` 只是为了绘图/显示，把同一个 `reference_col` 复制到整条数组里。
3. 你的两个权重函数里，`Cal_Weigth1()` 更偏近处，`Cal_Weigth2()` 更偏远处。原因是 `Weigth1` 的主要非零权重整体落在更大的行号上，而大行号对应更靠近图像底部。
4. 还要注意一个细节：两个权重函数实际都只累加 `i = 1..99`，所以即使数组长度是 120，最底下 `100..119` 这些行现在并没有参与加权。也就是说：
   - `Cal_Weigth1()` 实际有效区更偏中下部/近处；
   - `Cal_Weigth2()` 实际有效区更偏中上部/远处。
5. 如果你后面想把“近”和“远”再拉开一点，优先改循环区间和权重非零区间，而不只是改权重数值本身。




//===============================================================================================
时间：2026-04-22 17:56:09 CST
用户问题：帮我生成一个 .m 代码（MATLAB），内容是两种曲线，一种是之前的 k_dev_to_yaw 线性曲线，另一种是推荐的三次拟合曲线。
Codex建议：已生成一个 MATLAB 脚本 `compare_yaw_mapping.m`，默认对比 `y = k_dev_to_yaw * x` 和 `y = k1 * x + k3 * x^3`，并带有三次曲线限幅，便于直接观察线性和非线性映射在大偏差区域的差异。

时间：2026-04-22 17:38:31 CST
用户问题：修改前两个建议后已经不会掉头，但速度快时拐弯能力不足。我觉得有两个原因：一是反应太慢，权重可以给远一点；二是 k_dev_to_yaw 是固定值，想换成非线性函数，输入越大反馈越大，推荐什么函数比较好？
Codex建议：
1. 先处理“反应慢”，本质是预瞄太近。建议不要直接只用一个权重结果，先把近处和远处混合起来：`float e_near = Cal_Weigth1(); float e_far = Cal_Weigth2(); float deviation = 0.7f * e_near + 0.3f * e_far;`。如果速度更高，再试 `0.6 / 0.4`，不要一开始就把远处权重拉太大，否则直道容易发飘。
2. 非线性函数推荐优先用“线性 + 三次项”：`f(x) = k1 * x + k3 * x * x * x`。它比 `tanh/atan` 更适合你现在这个需求，因为你想要的是“小误差别太抖，大误差更猛”，而 `tanh/atan` 会在大误差区压缩输出。
3. 直接可试的写法：`float x = std::clamp(deviation, -1.0f, 1.0f); float vision_delta_yaw = 6.0f * x + 12.0f * x * x * x; vision_delta_yaw = std::clamp(vision_delta_yaw, -18.0f, 18.0f);`
4. 调参顺序：先固定 `k1 = 6`；再调 `k3`，建议范围 `8 ~ 14`；最后调总限幅 `15 ~ 22` 度。大弯还是进不去就加 `k3`，直道开始晃就减 `k1`。
5. 如果后面还嫌高速弯道反应慢，下一步不要继续硬加 `k3`，而是加“方向误差/赛道斜率”项，也就是用近点和远点的中线斜率来补一个 heading error，这通常比单纯放大横向偏差更有效。




//==========================================================================================================================
时间：2026-04-22 17:35:51 CST
用户问题：以后的你的建议都写在codex_tips文件里面，每一次都写在文本的最上面，并且写明我的问题还有时间
Codex记录：后续给你的建议将统一写入本文件，并且始终把最新一条插在最上面。
